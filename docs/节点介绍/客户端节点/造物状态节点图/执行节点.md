---
title: 执行节点
path_id: mhb09x012qu8
updated_at: 2026-08-26 16:46:36
category: 节点介绍/客户端节点/造物状态节点图
source_url: https://act.mihoyo.com/ys/ugc/tutorial/detail/mhb09x012qu8
---

# **一、通用**

## **1. 执行技能**

![](../../../images/cd1ead3ee4cd9114.undefined)

**节点功能**

执行技能序号对应的技能

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 技能序号 | 整数 |  |

## **2. 继续执行前一帧行为**

![](../../../images/ce41f61b4c4055b8.undefined)

**节点功能**

若前一帧执行战术未执行完毕，会继续执行，直到完毕

若前一帧执行技能，则不做处理

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
|  |  |  |  |

# **二、战术**

## **1. 战术：地面闲逛**

![](../../../images/f92f906bb1e9f81a.undefined)

**节点功能**

造物执行地面闲逛行为，在范围内随机行走

执行条件：

受到*造物降频*影响， 距离角色太远的造物不会进入本战术选到合适的目标点

选点规则：

跟大部分战术不同， *地面闲逛*战术的选点逻辑是在**可能**可以执行*地面闲逛*战术但还没进入的时候

因为选到合适的目标点是进入战术的条件之一

真正进入战术以后， 直接执行移动任务， 直到停止移动任务(到达目的地或者撞到障碍物了)后扣除冷却时间， 退出战术若造物当前位置已经不在*闲逛半径*范围内， *地面闲逛*战术就会选择从当前位置往出生位置的方向， 找距离为*闲逛半径*的点作为目标点如果支持使用寻路， 会使用寻路探测目标点合法性

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 速度 | 枚举 | 走、跑 |
| 入参 | 闲逛半径 | 浮点数 | 闲逛距离出生点的最远距离 |
| 入参 | 闲逛最小间隔时长 | 浮点数 | 闲逛冷却时间， 每次退出战术时， 都会在 [闲逛间隔最小时间， 闲逛间隔最大时间] 里随机一个时间， 作为冷却时间，冷却时间内不会执行本战术 |
| 入参 | 闲逛最大间隔时长 | 浮点数 |  |
| 入参 | 单次最小闲逛距离 | 浮点数 | 闲逛距当前点距离 |
| 入参 | 单次最大闲逛距离 | 浮点数 | 闲逛距当前点距离 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **2. 战术：地面追击**

![](../../../images/ba9b3676fc8f55d3.undefined)

**节点功能**

造物执行地面追击行为

追击目标战术，会自主选择目标周围合适的位置做目标点

执行条件：

【必须满足】

1.有目标2.自身不在空中3.自身或者目标在领地范围内

【任一满足】

1.当前不在本战术，且自身和目标距离在 [*触发追击的最小距离*，*触发追击的最大距离* ] 范围内，才会进入战术2.已经处于战术时，自身和目标距离大于*停止追击距离*才会继续执行战术

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 触发追击的最小距离 | 浮点数 |  |
| 入参 | 触发追击的最大距离 | 浮点数 |  |
| 入参 | 停止追击距离 | 浮点数 |  |
| 入参 | 外圈追击速度 | 枚举 | 走、跑 |
| 入参 | 内圈半径 | 浮点数 | 内圈分界半径 |
| 入参 | 内圈追击速度 | 枚举 | 走、跑 |
| 入参 | 单次追击持续时间 | 浮点数 | 若数值小于0，则不执行战术 |
| 入参 | 战术实例冷却时间 | 浮点数 |  |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **3. 战术：静止不动**

![](../../../images/c4d5787cd74e2372.undefined)

**节点功能**

造物执行静止不动行为

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **4. 战术：移动到目标点**

![](../../../images/52dc3a8f8cfd33f0.undefined)

**节点功能**

造物执行移动到目标点行为

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 目标点位置 | 三维向量 |  |
| 入参 | 到达判定距离 | 浮点数 |  |
| 入参 | 移动速度 | 枚举 | 走、跑 |
| 入参 | 转向速度 | 浮点数 |  |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **5. 战术：移动到目标实体**

![](../../../images/3ff8470e1bec8b6d.undefined)

**节点功能**

造物执行移动到目标实体行为

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 目标实体 | 实体 |  |
| 入参 | 到达判定距离 | 浮点数 | 与目标实体的距离小于等于配置值则视为到达 |
| 入参 | 移动速度 | 枚举 | 走、跑 |
| 入参 | 转向速度 | 浮点数 |  |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **6. 战术：旋转到指定朝向**

![](../../../images/6f66f4a6b1bfcb50.undefined)

**节点功能**

造物执行旋转到指定朝向行为

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 目标朝向 | 三维向量 |  |
| 入参 | 水平旋转角速度 | 浮点数 |  |
| 入参 | 是否使用旋转动画 | 布尔值 |  |
| 入参 | 旋转方向 | 枚举 | 默认：按最小角度方向  顺时针  逆时针 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **7. 战术：旋转指定角度**

![](../../../images/7a73852eb17f2356.undefined)

**节点功能**

造物执行旋转指定角度行为，角速度在实际运行中会有一定误差

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 指定角度 | 浮点数 |  |
| 入参 | 水平旋转角速度 | 浮点数 | 若配置正数，则顺时针旋转；  若配置负数，则逆时针旋转； |
| 入参 | 是否使用旋转动画 | 布尔值 | 某些造物在使用旋转动画且水平旋转角速度过小的时候可能会表现异常，请尝试调大水平旋转角速度或不使用旋转动画 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **8. 战术：地面逃跑**

![](../../../images/9c6347dcaa3f0cbd.undefined)

**节点功能**

造物执行地面逃跑行为，逃离目标的战术，试图背对目标并向远处移动。

允许配置多段逃跑移动，段数的取值范围为 [*逃跑最少段数*，*逃跑最多段数*) ，每段/每次会计算一个逃跑点位。

执行条件：

不在冷却时间中，自身在领地内，距离目标小于*逃跑触发距离*

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 逃跑触发距离 | 浮点数 |  |
| 入参 | 移动速度 | 枚举 | 走、跑 |
| 入参 | 逃跑最大角度 | 浮点数 | 以配置值绝对值的2倍，做实际逃跑选点范围 |
| 入参 | 逃跑最少段数 | 整数 |  |
| 入参 | 逃跑最多段数 | 整数 |  |
| 入参 | 逃跑距离最小值 | 浮点数 |  |
| 入参 | 逃跑距离最大值 | 浮点数 |  |
| 入参 | 逃离触发冷却时间 | 浮点数 |  |
| 入参 | 计时清除仇恨并退出战术 | 浮点数 | 若数值小于0，则不执行战术。  否则从进入战术开始计时, 超时直接强制脱战。  执行战术结束后，会马上触发脱战 |
| 入参 | 是否受到领地限制 | 布尔值 | 当配置为是，且领地内无合法的点位，会立刻结束该战术 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **9. 战术：脱战后返回出生点**

![](../../../images/a2a25f9306d4b5e6.undefined)

**节点功能**

造物执行脱战后返回出生点行为

脱离战斗时，回初始点位的战术。战术执行期间，仇恨系统是关闭的， 直到战术结束才会重新打开。

执行条件：

仅支持战前/脱战阶段，并且通常是放在战前的第一个战术，以保证仇恨系统脱战后马上能进入该战术

目标点选择：

目标点选择时机为进入战术那一帧， 不会在战术执行期间修改目标

以下条件从前往后顺序检查， 若满足对应条件， 则按该条内容确定目标点

【执行巡逻】战术执行过，并且配置了*结束时点位设置为出生点*为是时，会以新的出生点作为目标点选择出生点为目标点

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 移动速度 | 枚举 |  |
| 入参 | 是否传送回出生点 | 布尔值 |  |
| 入参 | 强制触发传送距离 | 浮点数 | 触发前提，需要*是否传送回出生点*配置为是  当自身距离出生点位置大于等于配置距离时，会强制触发传送 |
| 入参 | 强制触发传送时间 | 浮点数 | 触发前提，需要*是否传送回出生点*配置为是  战术持续时间超过配置时间，仍然未回到出生点，会强制触发传送 |
| 入参 | 屏蔽脱战恢复生命值 | 布尔值 |  |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **10. 战术：地面对峙**

![](../../../images/cd89fdd33a23a02c.undefined)

**节点功能**

造物执行地面对峙行为，四向移动战术，包括静止/前/后/左/右五种方向，可用于来营造与玩家的相持对峙感。

可以通过概率配置创造出只左右绕玩家移动/喜好前进厌恶后退等移动行为，也包含一些与玩家距离调整和避障的配置。

本质上是决策出一个方向，并不会有一个具体的目标点

执行条件：

需要自身或目标在领地范围内当*是否一定寻路可达*配置为是且不处在空中时，若寻路失败会退出战术

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 对峙距离内圈半径 | 浮点数 |  |
| 入参 | 对峙距离外圈半径 | 浮点数 |  |
| 入参 | 改变方向时间间隔 | 浮点数 | 换方向过渡时间，默认为0。每次方向改变，均会使造物静止该配置时间，随后换到新方向  从其他方向进休息、从休息进别的方向，都不算换方向 |
| 入参 | 休息间隔时间最小值 | 浮点数 |  |
| 入参 | 休息间隔时间最大值 | 浮点数 |  |
| 入参 | 是否一定寻路可达 | 布尔值 |  |
| 入参 | 是否受到领地限制 | 布尔值 | 当配置为是，且领地内无合法的点位，会立刻结束该战术 |
| 入参 | 移动取消触发权重 | 浮点数 |  |
| 入参 | 往前移动权重 | 浮点数 | 只影响常规模式下的移动权重，把某个方向的权重降为0，也可能因为避障/异常情况进入该方向 |
| 入参 | 往后移动权重 | 浮点数 | 同上 |
| 入参 | 往左移动权重 | 浮点数 | 同上 |
| 入参 | 往右移动权重 | 浮点数 | 同上 |
| 入参 | 左右避障距离 | 浮点数 | 当配置数值小于0，则不执行避障功能 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **11. 战术：执行巡逻**

![](../../../images/5ceaf454fba0d23f.undefined)

**节点功能**

造物执行巡逻行为，依据造物自身配置的巡逻模板来进行移动表现的战术。

执行条件：

造物自身配置了巡逻模板战术引用的巡逻模板，引用的路径数据不为空

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 巡逻模板序号 | 整数 |  |
| 入参 | 是否从最近路点开始 | 布尔值 |  |
| 入参 | 结束时点位设置为出生点 | 布尔值 | 会影响*脱战后返回出生点*战术 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **12. 战术：旋转朝向目标实体**

![](../../../images/c5af71b011d4e279.undefined)

**节点功能**

造物执行旋转朝向目标实体行为

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 |  |
| 入参 | 目标实体 | 实体 |  |
| 入参 | 水平旋转角速度 | 浮点数 |  |
| 入参 | 是否使用旋转动画 | 布尔值 |  |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |

## **13. 战术：空中降落**

![](../../../images/df1a238568c55d71.undefined)

**节点功能**

造物执行空中降落行为，根据配置角度计算出落地点，向落地点移动

当前位置与落地点的高度差小于 *判定降落时的离地高度* 时，视为战术执行完成

执行条件：

自身具有单位状态效果：*造物浮空*

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 | 默认为是 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |
| 入参 | 降落方向与水平面最小夹角 | 浮点数 |  |
| 入参 | 降落方向与水平面最大夹角 | 浮点数 |  |
| 入参 | 判定降落时的离地高度 | 浮点数 | 相对地面高度 |
| 入参 | 移动速度 | 枚举 | 走、跑 |
| 入参 | 覆写移动速度 | 浮点数 |  |
| 入参 | 覆写转向速度 | 浮点数 |  |
| 入参 | 降落最大持续时间 | 浮点数 | 若数值小于0，则不执行战术 |

## **14. 战术：空中追击**

![](../../../images/2279f6ce8b952496.undefined)

**节点功能**

造物执行空中追击行为

追击目标战术，会自主选择目标周围合适的位置做目标点

执行条件：

【必须满足】

1.自身具有单位状态效果：*造物浮空*2.有目标3.自身入战设置需要配置视野检测区域

【任一满足】

1.当前不在本战术，且自身和目标水平距离大于 *触发追击的水平距离* 或高度差大于 *触发追击的高度差* 时，才会进入战术2.已经处于战术时，自身和目标水平距离大于 *停止追击的水平距离* 或高度差大于 *停止追击的高度差* 时，才会继续执行战术

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 | 默认为是 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |
| 入参 | 触发追击的水平距离 | 浮点数 |  |
| 入参 | 停止追击的水平距离 | 浮点数 |  |
| 入参 | 是否使用高度差追击 | 布尔值 | 当配置为否时，*触发追击的高度差*和*停止追击的高度差*均不生效 |
| 入参 | 触发追击的高度差 | 浮点数 | 若数值小于0，则不生效 |
| 入参 | 停止追击的高度差 | 浮点数 | 同上 |
| 入参 | 相对目标高度偏移下限 | 浮点数 | 目标点的高度偏移范围 |
| 入参 | 相对目标高度偏移上限 | 浮点数 | 同上 |
| 入参 | 移动速度 | 枚举 | 走、跑 |
| 入参 | 覆写移动速度 | 浮点数 |  |
| 入参 | 覆写转向速度 | 浮点数 |  |
| 入参 | 单次追击最大持续时间 | 浮点数 | 若数值小于0，则不执行战术 |
| 入参 | 战术实例冷却时间 | 浮点数 |  |

## **15. 战术：空中逃跑**

![](../../../images/6a9aa2a35746b772.undefined)

**节点功能**

造物执行空中逃跑行为，逃离目标的战术，试图背对目标并向远处移动。

允许配置多段逃跑移动，段数的取值范围为 [*逃跑最少段数*，*逃跑最多段数*)

每段/每次会计算一个逃跑点位，支持方位角和俯仰角的配置。

执行条件：

自身具有单位状态效果：*造物浮空*不在冷却时间中自身在领地内距离目标小于*逃跑触发距离*

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 | 默认为是 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |
| 入参 | 逃跑触发距离 | 浮点数 |  |
| 入参 | 移动速度 | 枚举 | 走、跑 |
| 入参 | 覆写移动速度 | 浮点数 |  |
| 入参 | 覆写转向速度 | 浮点数 |  |
| 入参 | 逃跑最大方位角 | 浮点数 | 以配置值绝对值的2倍，做实际逃跑选点范围 |
| 入参 | 逃跑最小俯仰角 | 浮点数 | 逃跑俯仰角范围，上负下正 |
| 入参 | 逃跑最大俯仰角 | 浮点数 | 同上 |
| 入参 | 逃跑最少段数 | 整数 |  |
| 入参 | 逃跑最多段数 | 整数 |  |
| 入参 | 逃跑距离最小值 | 浮点数 |  |
| 入参 | 逃跑距离最大值 | 浮点数 |  |
| 入参 | 向上逃离权重 | 浮点数 |  |
| 入参 | 向下逃离权重 | 浮点数 |  |
| 入参 | 逃跑完成/被阻挡时是否转向目标 | 布尔值 | 默认为是 |
| 入参 | 逃离触发冷却时间 | 浮点数 |  |
| 入参 | 计时清除仇恨并退出战术 | 浮点数 | 若数值小于0，则不执行战术。  否则从进入战术开始计时, 超时直接强制脱战。  执行战术结束后，会马上触发脱战 |
| 入参 | 是否受到领地限制 | 布尔值 | 当配置为是，且领地内无合法的点位，会立刻结束该战术 |

## **16. 战术：空中对峙**

![](../../../images/543d7b0dd39923a0.undefined)

**节点功能**

造物执行空中对峙行为，空间中的多向移动战术，能基于配置的目标周围概率空间随机选取位置移动。

能在三维空间中实现追击/远离/左右绕玩家移动等表现，也包含一些与玩家距离调整和避障的配置。

执行条件：

自身具有单位状态效果：*造物浮空*自身或目标在领地范围内自身当前预设状态支持六向移动

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 | 默认为是 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |
| 入参 | 对峙距离内圈半径 | 浮点数 |  |
| 入参 | 对峙距离外圈半径 | 浮点数 |  |
| 入参 | 对峙最小高度 | 浮点数 | 相对地面高度 |
| 入参 | 对峙最大高度 | 浮点数 | 同上 |
| 入参 | 相对地面的最适高度 | 浮点数 | 最适高度与实际高度的差值越大，往反方向移动的概率越大 |
| 入参 | 休息间隔时间最小值 | 浮点数 |  |
| 入参 | 休息间隔时间最大值 | 浮点数 |  |
| 入参 | 是否受到领地限制 | 布尔值 | 当配置为是，且领地内无合法的点位，会立刻结束该战术 |
| 入参 | 是否受到参考地面的高度限制 | 布尔值 | 当配置为是，或*相对地面的最适高度*大于0时，*对峙最小/最大高度*才会生效 |
| 入参 | 移动取消触发权重 | 浮点数 |  |
| 入参 | 往前移动权重 | 浮点数 | 只影响常规模式下的移动权重，把某个方向的权重降为0，也可能因为避障/异常情况进入该方向 |
| 入参 | 往后移动权重 | 浮点数 | 同上 |
| 入参 | 往左移动权重 | 浮点数 | 同上 |
| 入参 | 往右移动权重 | 浮点数 | 同上 |
| 入参 | 往上移动权重 | 浮点数 | 同上 |
| 入参 | 往下移动权重 | 浮点数 | 同上 |
| 入参 | 左右避障距离 | 浮点数 | 当配置数值小于0，则不执行避障功能 |
| 入参 | 避障上飞高度 | 浮点数 |  |
| 入参 | 移动方向与垂直方向的最大避障夹角 | 浮点数 | 移动方向与垂直方向夹角大于该角度时，不进行上飞绕路而是直接停止 |
| 入参 | 覆写移动速度 | 浮点数 |  |
| 入参 | 覆写移动转向速度 | 浮点数 |  |
| 入参 | 覆写原地转向速度 | 浮点数 |  |
| 入参 | 移动速度 | 枚举 | 走、跑 |
| 入参 | 单次最小移动距离 | 浮点数 |  |
| 入参 | 单次最大移动距离 | 浮点数 |  |

## **17. 战术：空中闲逛**

![](../../../images/71c5fc0e1a858c5e.undefined)

**节点功能**

造物执行空中闲逛行为，在范围内随机移动

执行条件：

自身具有单位状态效果：*造物浮空*受到*造物降频*影响， 距离角色太远的造物不会进入本战术选到合适的目标点

选点规则：

跟大部分战术不同， *空中闲逛*战术的选点逻辑是在**可能**可以执行*空中闲逛*战术但还没进入的时候

因为选到合适的目标点是进入战术的条件之一

真正进入战术以后， 直接执行移动任务， 直到停止移动任务(到达目的地或者撞到障碍物了)后扣除冷却时间， 退出战术若造物当前位置已经不在 *闲逛半径* 或 *相对出生位置高度限制* 范围内，或多次无法选到合适的目标点， *空中闲逛*战术就会选择默认目标点作为当前目标点默认目标点：与出生位置的高度偏移量为 *相对出生位置最小与最大高度均值* 的点

**节点参数**

|  |  |  |  |
| --- | --- | --- | --- |
| **参数类型** | **参数名** | **类型** | **说明** |
| 入参 | 是否执行 | 布尔值 | 默认为是 |
| 入参 | 战术上下文 | 字符串 |  |
| 入参 | 是否可以打断技能 | 布尔值 | 默认为否 |
| 入参 | 移动速度 | 枚举 | 走、跑 |
| 入参 | 覆写移动速度 | 浮点数 |  |
| 入参 | 覆写转向速度 | 浮点数 |  |
| 入参 | 闲逛半径 | 浮点数 | 闲逛距离出生点的最远距离 |
| 入参 | 相对出生点最小高度 | 浮点数 |  |
| 入参 | 相对出生点最大高度 | 浮点数 |  |
| 入参 | 闲逛最小间隔时长 | 浮点数 | 闲逛冷却时间， 每次退出战术时， 都会在 [*闲逛最小间隔时长*，*闲逛最大间隔时长*] 里随机一个时间， 作为冷却时间，冷却时间内不会执行本战术 |
| 入参 | 闲逛最大间隔时长 | 浮点数 |  |
| 入参 | 单次闲逛最大持续时间 | 浮点数 | 单次闲逛超出该时长时自动停止 |
| 入参 | 单次最小闲逛距离 | 浮点数 | 闲逛距当前点距离 |
| 入参 | 单次最大闲逛距离 | 浮点数 | 闲逛距当前点距离 |
